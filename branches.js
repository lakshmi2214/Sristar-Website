
        // Initialize WOW.js for scroll-triggered animations
        new WOW().init();

        document.addEventListener('DOMContentLoaded', () => {
            // The 'active' class is already applied to the second box in the HTML for demonstration.
            // You can add more dynamic behavior here if needed.
        });